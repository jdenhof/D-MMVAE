# experiments.yaml

experiment_name: bce_testing
run_name: assay_species_weight_40_10_epochs
train_command:
  fit:
    model: configs/model/adv_trials/assay_species.yaml
    trainer:
      track:
        # test: configs/trainer/config.test.yaml
        full: configs/trainer/config.yaml
    data:
      track:
        local: configs/data/local_chunk.yaml
        # server: configs/data/server.yaml


categories:
- 'donor_id'
- 'assay'
- 'dataset_id'
- 'cell_type'
- 'species'


merge_keys:
- z
